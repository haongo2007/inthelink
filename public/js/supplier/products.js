jQuery(document).ready(function($) {
	/*$('.custom-switch-btn').click(function(event) {
        var id = $(this).attr('id');
        var token = window.token;
        $.ajax({
            url: window.base_url+'supplier/products/update/status',
            type: 'PUT',
            data: {id: id},
        })
    });*/
});